server:
  port: 8060

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/short_url?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_PASSWORD:root123}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password: redis123
      database: 1
      timeout: 10000

short-url:
  host: http://localhost:8060
  jwt:
    secret: MySecretKeyForJWT2026LocalDevEnvironmentThatIsAtLeast256BitsLong
    expiration: 86400000

logging:
  level:
    top.naccl.dwz: debug
